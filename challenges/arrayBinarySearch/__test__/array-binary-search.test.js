'use strict';

const arrayBinarySearch = require('../array-binary-search.js');

describe('array-binary-search.js', () => {
  it('exists', () => {
    expect(arrayBinarySearch).toBeTruthy();
  });
});
